# Отчёт: расписание-фолбэк, Фаза 1 (список плановых прибытий)

Ветка: `feature/gtfs-freshness` · Дата: 2026-07-14 · Статус: **Фаза 1 готова, на
staging. [STOP & ASK] перед Фазой 2 (карта scheduled-объектов). Контракт:
`docs/SCHEDULE_FALLBACK_CONTRACT.md`. Флаг `schedule_fallback` (OFF prod / ON
staging). В main/прод НЕ мержил.**

## Что делает
Заполняет дыру в ките «прибытия»: когда живого потока мало (ночь/межпик), экран
остановки больше не пустой — показывает плановые прибытия из GTFS-расписания,
как у Яндекса/Belgrade Bus. «Живой первый + расписание хвостом».

## Архитектура (precompute — воркер не парсит 110 МБ на запрос)
- **`build-schedule.mjs`** (в цепочке `npm run gtfs:build`): по каждой **городской**
  остановке — плановые отправления каждой линии/направления, сгруппированы по
  сервису RD/S/N. Город+пригород (600-е из пригородной версии, без задвоения).
  Формат: **минуты от полуночи** (overnight ≥1440 сохранены). Раздача per-stop
  (как shapes): `public/gtfs/schedule/<stop_id>.json`.
- **Размер (замерено, твой гейт):** 3239 файлов, 2.33 млн отправлений, **raw
  10.7 МБ / ~3.9 МБ gzip суммарно**; **per-request ~3.5 КБ raw / ~1 КБ gzip** —
  копейки на запрос (аудитория на мобильном). Минуты вместо секунд дали −26%.
- **`schedule/_meta.json`**: календарь (RD/S/N) + исключения `calendar_dates`
  (праздники), для резолва активного сервиса на дату.

## Логика (`src/lib/schedule.ts`, чистая/тестируемая)
- **Сервис-на-дату:** будни→RD, сб→S, вс→N + `calendar_dates` (праздник RD→N).
- **Таймзона:** «сейчас» считается в `Europe/Belgrade` (лето UTC+2 / зима +1).
- **Overnight:** времена ≥1440 (до 26:xx) от **вчерашнего** сервиса показываются
  в ранние часы сегодня; сегодняшний overnight (~24ч вперёд) отсекается потолком.
- **Окно:** следующие **3 рейса на линию ИЛИ 90 мин** (что больше), потолок 3ч,
  ≤10 на линию.
- **Дедуп live↔scheduled:** гасим плановый рейс, ближайший к каждому живому ТС на
  том же **направлении** (`direction_route_id` из фикса геометрии) в пределах
  20 мин; поздние плановые остаются хвостом. Не «есть живой → скрыли всё».

## Контракт
`ArrivalDto.source:"scheduled"` (аддитивно; live без изменений). Эмиссия за
флагом `schedule_fallback`. Экран остановки помечает плановые строки
(`arrivalScheduled` EN/RU/SR, приглушённо, иконка часов); пустого состояния нет,
когда по расписанию что-то идёт.

## Тесты
- Backend 75/75 (новые: `schedule.test.ts` — сервис-по-дате вкл. праздник,
  таймзона лето/зима, overnight, окно 3-или-90, дедуп; +флаг).
- Flutter 122/122 (+рендер scheduled-строки). analyze/tsc чисто.

## Проверка на staging
`https://6bc7e468-...workers.dev/api/v1/arrivals?stop=20091` → живой 304N (30м) +
плановые хвостом (304N 102м, 304Nb 106м); дедуп не задвоил. Ночью корректно идут
ночные линии (overnight-сервис резолвится).

## Как проверить (без терминала)
На **https://preview-gtfs-freshness.stigla.pages.dev** (флаг ON) открой любую
остановку — даже ночью/в межпик список не пустой: живые прибытия сверху, плановые
ниже с пометкой «По расписанию» (иконка часов, приглушённо). Сравни с продом
(флаг OFF — плановых нет).

## Дальше — [STOP & ASK] перед Фазой 2 (карта)
Фаза 2: scheduled-объекты **на карте** (`VehicleDto.trajectory`+`as_of`, движение
по timed-плану, дедуп по рейсу). Нужен per-trip индекс геометрии+времён (тяжелее
per-stop) — замерю размер до вливания. Берусь после твоего ревью списка на staging.

## Фаза 2 — scheduled-объекты на карте (2026-07-14, на staging, [STOP & ASK] перед прод)

Backend-эмиссия по контракту; фронт-рендер — в символьной ветке (готов).

- **Индекс (замер, гейт):** `build-trips.mjs` → `public/gtfs/trips/<route_id>.json`
  (per-route, времена рейсов выровнены по остановкам shape). **797 файлов,
  93 555 рейсов, raw 14.6 МБ / gzip 2.3 МБ** (~19 КБ/route). Тянется **только для
  маршрутов линий БЕЗ живого ТС** в зоне (кап 8) → в плотном live-центре почти не
  читается.
- **Эмиссия** (`vehicles.ts`): для линий кадра без live находим рейсы, что **сейчас
  по графику в пути** (`scheduledMapObjectsForRoute`), интерполируем позицию между
  двумя текущими остановками, отдаём `source:"scheduled"`, `trip_id`, `as_of`,
  `heading`, `trajectory:[{lat,lon,eta_seconds}]` (кумулятивно от `as_of`) — как
  live. Дедуп по (линия+направление): где есть live — scheduled не эмитим.
- **Самоограничение подтверждено:** окраина/разреженная зона → scheduled есть
  (напр. 12 объектов); плотный центр днём → 0 (всё покрыто live). Мобильный
  трафик не страдает.
- **Найден и починен баг:** фан-аут nearby + per-route фетчи пробивали лимит
  подзапросов воркера → эмиссия молча давала 0. Фикс: фан-аут не считает Phase-1
  список (`includeScheduleList=false`), trips/shapes кэшируются в isolate, кап 8.
- **Тесты:** backend 79/79 (map-объекты: интерполяция in-transit, trajectory-eta,
  сервис-по-дате, overnight). Флаг `schedule_fallback` (тот же).

**Проверка на staging (API):** `/vehicles/nearby` на окраине отдаёт
scheduled-объекты с корректным `trajectory` (eta 0→60→180…, монотонно). Визуальный
рендер — в символьной ветке; чтобы увидеть машины на карте, staging символьной
ветки нужно направить на этот backend-версию-URL.

**Статус:** на staging, в прод НЕ мержил — жду ревью и команды.
