# Отчёт: галерея дизайн-слепков как часть Definition of Done

**Дата:** 2026-07-12
**Ветка:** `fix/gallery-dod`
**Задача:** `docs/prompts/prompt_gallery_dod.md`

## Контекст

`feature/design-gallery` влита в `main` (коммит `4e272a7`), галерея живёт в
`design/gallery/` и задеплоена на `preview-gallery`. Предусловие задачи
выполнено. Цель — сделать поддержание галереи частью завершения каждой задачи,
меняющей UI, и оставить будущим сессиям инструкцию. Правились **только
документы**, код не тронут.

## Что сделано

### 1. `CLAUDE.md` → «Завершение задачи (definition of done сессии)»

Добавлен новый пункт **4** (между прежними 3 и 4), нумерация сдвинута
(бывший 4 → 5, 5 → 6): если задача добавляет/меняет видимые экраны или
состояния UI — обновить страницы в `design/gallery/` и передеплоить
`preview-gallery`; правила — в `design/gallery/README.md`; задачи без визуальных
изменений галерею не трогают.

### 2. `docs/WORKFLOW.md`

- В схему «Цикл задачи» после «работа → отчёт… + коммиты» вставлен шаг
  `→ обновить design/gallery при визуальных изменениях`.
- В «Дерево решений» → «Приёмка задачи» добавлена строка: если задача меняла UI —
  сверять, что слепки галереи обновлены и `preview-gallery` передеплоен.

### 3. `design/gallery/README.md` (новый файл)

Инструкция для будущих сессий: назначение (слепок UI для html.to.design,
DOM → слои, JS запрещён), когда обновлять (новое состояние → страница; правка →
правка; удаление → удаление из галереи и `index.html`), правила страниц
(390×844, kebab-case, семантичные классы, мок-данные Белграда, карта = подложка +
DOM-маркеры, бейджи `branch`/`spec`/`flag`), команда деплоя `preview-gallery`
(выполняет сессия) и требование строки про галерею в отчёте.

### 4. `docs/BACKLOG.md`

В раздел «В проде (`main`)» добавлен пункт ✅ о галерее дизайн-слепков со ссылкой
на отчёт `feature/design-gallery` и на `design/gallery/README.md`. Статус сверен
по факту: ветка влита в main.

## Ограничения соблюдены

Тронуты только документы (`CLAUDE.md`, `docs/WORKFLOW.md`, `docs/BACKLOG.md`,
новый `design/gallery/README.md`, этот отчёт и промпт). Код `app/` и `backend/`
не менялся. Текст публичный: секретов, ключей и инфра-деталей нет. Merge в `main`
по ветке `fix/gallery-dod` не делался — только по явной команде владельца.

---

## Как проверить (для владельца, без терминала)

1. **CLAUDE.md** → раздел «Завершение задачи (definition of done сессии)»: теперь
   6 пунктов, новый **пункт 4** — про обновление `design/gallery/` при
   визуальных изменениях.
2. **docs/WORKFLOW.md** → блок «Цикл задачи»: в схеме появилась строка
   `→ обновить design/gallery при визуальных изменениях`. В «Дереве решений» →
   «Приёмка задачи» — добавлено про сверку слепков галереи.
3. **Новая инструкция:** `design/gallery/README.md` — правила поддержания
   галереи (когда обновлять, правила страниц, деплой, отчётность).
4. **docs/BACKLOG.md** → «В проде (`main`)»: новый пункт ✅ про галерею
   дизайн-слепков.
5. **Сама галерея (не менялась этой задачей):**
   https://preview-gallery.stigla.pages.dev — под тем же паролем, что остальные
   `*.pages.dev`-превью (доступы — `docs/staging.md`).
